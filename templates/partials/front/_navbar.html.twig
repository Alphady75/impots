<nav class="navbar navbar-expand-lg bg-white navbar-light d-none d-md-block">
    <div class="container">

        <!-- Brand -->
        <a class="navbar-brand text-light font-weight-bold text-dark" href="{{ path('app_home') }}">
            <img src="{{ asset('img/logo.png') }}" height="50px">
        </a>

        <!-- Toggler/collapsibe Button -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar links -->
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
            {% if app.request.attributes.get('_route') == 'test_eligibilite' %}

            {% else %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item {{ app.request.attributes.get('_route') == 'test_eligibilite' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('test_eligibilite') }}">Reduire vos impôts</a>
                    </li>
                    <li class="nav-item {{ app.request.attributes.get('_route') == 'app_blog' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('app_blog') }}">S'informer</a>
                    </li>
                    <li class="nav-item {{ app.request.attributes.get('_route') == 'app_evenements' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('app_evenements') }}">Evénements</a>
                    </li>
                    <li class="nav-item {{ app.request.attributes.get('_route') == 'pages_apropos' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('pages_apropos') }}">A propos</a>
                    </li>
                    <li class="nav-item {{ app.request.attributes.get('_route') == 'pages_contact' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('pages_contact') }}">Contact</a>
                    </li>
                </ul>

            {% endif %}
        </div>

    </div>
</nav>