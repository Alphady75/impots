{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Accueil {% endblock %}

{% block body %}

<!-- Home section -->

{{ include('questionnaire/_test_eligibilite_section.html.twig') }}

{{ include('home/_about_section.html.twig') }}

{{ include('home/_temoignage_section.html.twig') }}

<!-- Blog Area -->
<section class="home2-blog-area section-padding">
    <div class="container">
        <div class="blog-top-title mb-4">
            <div class="row align-items-center">
                <div class="col-lg-10">
                    <div class="bixol-title-area">
                        <h2>Découvrez nos articles documentés <br>pour répondre à toutes vos questions</span></h2>
                    </div>
                </div>
            </div>
        </div>
        <div class="home2-blog-slider" style="margin-top: 90px!important;">
            <div class="row">
                {% for post in lastposts() %}

                <div class="col-md-4 d-flex align-content-stretch">
                    {{ include('blog/_bloc_post.html.twig') }}
                </div>

                {% else %}

                <div class="col-12"><p>Aucune news pour l'instant</p></div>

                {% endfor %}
            </div>
        </div>
    </div>
</section>
<!-- Blog Area End -->

{{ include('home/_faq_section.html.twig') }}

{% endblock %}
